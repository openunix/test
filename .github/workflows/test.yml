name: Test Workflow
on:
  pull_request:
    types: [ "closed" ]
  workflow_dispatch:

jobs:
  test-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          cat README.md
